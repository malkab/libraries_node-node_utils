# Dev database connection
ssh:
  kepler-remote:
    host: the_default_host
    port: the_default_port

dev_db:
  default:
    host: the_default_host
    port: the_default_port
    user: the_default_user
    pass: the_default_pass
  kepler-remote:
    host: SYSTEM.kepler.host
    port: SYSTEM.kepler.whatever_db.port

production_db:
  default:
    host: the_default_host
    port: the_default_port
    user: the_default_user
    pass: the_default_pass
  kepler-remote:
    host: SYSTEM.kepler.host
    port: SYSTEM.kepler.whatever_db.port
  kepler-localhost:
    host: SYSTEM.kepler.host
    port: SYSTEM.kepler.whatever_db.port
